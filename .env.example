# Founders Lab chatbots — Gemini API
# Copy to .env.local and fill in your key (https://aistudio.google.com/apikey).
# Without a key the chatbots run on their built-in offline demo engines.
VITE_GEMINI_API_KEY=
# Optional, defaults to gemini-2.5-flash
VITE_GEMINI_MODEL=
